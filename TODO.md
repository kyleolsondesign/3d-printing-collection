- [ ] Support drag/drop in the queue to reorder
- [ ] Saved searches / smart collections: Allow saving search queries + filter combinations as named collections (e.g., "Unpainted toys", "Large prints to try"). These would appear as quick-access buttons or a sidebar section.
- [x] Any time a change is made to a model, whether it's via a button on the card/table row or within a modal, all instances of that model should update on the current page. For example, on the designer page card view if you click "printed", it is not reflected in the UI
- [x] Be consistent with icons across all buttons. For instance the queue icon has the hamburger lines in some places, and the + and a check mark on the browse page. In general all the icons for queue are bad. Let's figure something better out
- [x] Add a new bulk recategorization feature. It should be able to search/filter through all models, bulk-select, and recategorize. It should allow creating new categories entirely, splitting off from old ones. Plan out a good user experience for this feature and make sure the code paths are generic enough that we can have some automated recategorization later on, with manual user approval.
- [x] Paid model categorization: Paid should be a tag and we should keep the current folder structure for paid models, but "Paid" itself is not a category.
- [x] Bulk purge mode: a feature under the settings maintenance area where you suggest models to delete (move to the macOS trash), based on an algorithm we determine via AskUserQuestion and can fine tune over time, including the aged of the model, if it was soft deleted (and the model folder or its parent does not exist still associated with a non-deleted model), marked as a bad print, etc. Also add a button in the model modal to mark for purge, which soft deletes it and implies it will be part of the next purge. Require extra confirmation before we purge anything under the Paid folder, or containing the paid tag
- [x] Make the search bar on the stats page follow the same behavior as the settings page, where it will take you to the browse view and search there if you enter a query
